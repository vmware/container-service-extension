# Valid config file
# Fill in fields marked with '???'

test:
  teardown_installation: true                              # Affects test_cse_server.py.
                                                           #   if true, delete all installation entities (even on test failure).
                                                           #   if false, do not delete installation entities (even on test success).
                                                           #   if this key is omitted, defaults to true.
  teardown_clusters: true                                  # Affects test_cse_client.py.
                                                           #   if true, delete test cluster (env.TEST_CLUSTER_NAME) on test failure.
                                                           #   if false, do not delete test cluster on test failure.
                                                           #   if this key is omitted, defaults to true.
                                                           # Successful client tests will not leave clusters up.
  test_all_templates: false                                 # Affects test_cse_client.py.
                                                           #   if true, tests cluster deployment on all templates found.
                                                           #   if false, tests cluster deployment only for first template found.
                                                           #   if this key is omitted, defaults to false.
  test_templates: "ubuntu-16.04_k8-1.19_weave-2.6.5:3"                                    # Tests will only create these templates if test_all_templates is set to false
                                                           #   format -> "template_1_name:template_1_revision,template_2_name:template_2_revision"
  upgrade_template_repo_url: ''                         #
  network: 'mynet'                                           # org network within @vdc that will be used during testing
                                                           #   Should have outbound access to the public internet
  org: 'myorg'                                               # vCD org where all the test will be run
  storage_profile: 'Development'                                   # name of the storage profile to use while creating clusters on this org vdc
  vdc: 'myorgvdc'                                               # Org VDC powering the org

mqtt:
  verify_ssl: false

vcd:
  host: bos1-vcloud-static-174-57.eng.vmware.com
  log: true
  password: ca$hc0w
  port: 443
  username: administrator
  verify: false

vcs:
- name: vc1
  password: Welcome@123
  username: administrator@vsphere.local
  verify: false

service:
  enforce_authorization: false
  legacy_mode: false
  log_wire: false
  processors: 15
  telemetry:
    enable: true

broker:
  catalog: cse
  default_template_name: ubuntu-16.04_k8-1.19_weave-2.6.5
  default_template_revision: 3
  ip_allocation_mode: pool
  network: csenet
  org: cseorg
  remote_template_cookbook_url: https://raw.githubusercontent.com/Anirudh9794/container-service-extension-templates/vcda-2546/template_v2.yaml
  storage_profile: 'Development'
  vdc: cseorgvdc
